@startuml ..\コンポーネント図_LogWriter.png

interface "呼び出し元" as caller

component "CommonTool.FileIO.dll" as FileIO {
  rectangle LogWriter {
    (ログファイルに書き込む)
  }
  rectangle TextWriter {
    (ファイルの存在チェック)
    (テキストファイル出力)
  }
}

component "外部部品" as OtherLogWriter

file ".log" as LogFile

caller --> (ログファイルに書き込む)

(ファイルの存在チェック) <-- LogWriter
(テキストファイル出力) <-- LogWriter

(テキストファイル出力) --> LogFile

LogWriter --> OtherLogWriter
OtherLogWriter --> LogFile

@enduml
